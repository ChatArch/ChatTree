[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ChatTree"
dynamic = ["version"]
description = "ChatTree: ChatArch placeholder package with a truthful CLI tree."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = ["click>=8.0"]
authors = [{name = "ChatArch", email = "1073853456@qq.com"}]
keywords = ["chattree", "chatarch", "cli"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://github.com/ChatArch/ChatTree"
Repository = "https://github.com/ChatArch/ChatTree"
Documentation = "https://arch.gh.wzhecnu.cn/ChatTree/"

[project.scripts]
chattree = "chattree.cli:main"

[project.optional-dependencies]
dev = ["build", "pytest", "twine"]
docs = ["mkdocs>=1.6,<2.0", "mkdocs-material>=9.5,<10.0", "mkdocs-static-i18n>=1.2,<2.0", "mike>=2.0,<3.0"]

[tool.setuptools.dynamic]
version = {attr = "chattree.__version__"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools]
include-package-data = true
